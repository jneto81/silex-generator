{% extends 'default.html.twig' %}

{% block content %}

<h1>Network edit</h1>

{% if form.vars.errors|length %}
<div class="alert alert-error">
  <button type="button" class="close" data-dismiss="alert">&times;</button>
  {{ form_errors(form) }}
</div>
{% endif %}

<form action="{{ path('network_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_rest(edit_form) }}
    <p>
        <button class="btn btn-small btn-primary" type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions unstyled container inline">
    <li>
        <a class="btn btn-small" href="{{ path('network') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('network_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button class="btn btn-small btn-danger" type="submit">Delete</button>
        </form>
    </li>
</ul>

{% endblock %}